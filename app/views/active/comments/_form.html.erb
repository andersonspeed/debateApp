<%= form_with(model: [@topic, @topic.comments.build], local: true) do |form| %>
  <div class="field">
    <%= form.label "Enter Reply" %>
    <%= form.text_area :content %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
